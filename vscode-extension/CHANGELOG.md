# Changelog

## 0.1.0

- Initial release
- Test Explorer integration with suite and test discovery
- CodeLens "Run Suite" and "Run Test" inline buttons
- JSON Schema validation for `.tat.json` files
- `tat: Validate File` command (no HTTP requests)
- Output channel for raw `tat` CLI output
