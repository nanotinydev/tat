// Re-exported from the CLI package — single source of truth.
// These are compile-time only; they do not exist in the bundled CJS output.
export type {
  AssertionResult,
  TestResult,
  SuiteResult,
  RunResult,
} from '../../tat-cli/src/types';
